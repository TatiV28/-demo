// backend/services/paymentService.js
const stripe = require('stripe')(process.env.STRIPE_SECRET_KEY);

const createPaymentIntent = async (amount, currency = 'rub') => {
  return await stripe.paymentIntents.create({
    amount: amount * 100,
    currency,
  });
};
